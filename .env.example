RDP_USER=oauth
# Leave blank to generate a password at container startup.
RDP_PASSWORD=
HATCH_START_URL=about:blank
# Normally leave this blank. Only use --no-sandbox as a last-resort workaround.
CHROMIUM_EXTRA_FLAGS=
